# bundler config.
/.bundle

# the default SQLite database.
/db/*.sqlite3
/db/*.sqlite3-journal

# all logfiles and tempfiles.
/log/*.log
/tmp

# uploaded runs, Twitch oauth credentials, etc.
/private

# precompiled assets
/public/assets

# testing
/.rspec
/coverage

node_modules
/public/packs
/public/packs-test
/node_modules
yarn-debug.log*
yarn-error.log*
.yarn-integrity
.env
.envrc

.DS_Store

# the .seed file prevents data from being seeded again; see Makefile
.seed

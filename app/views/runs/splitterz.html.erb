<%= @run.title %>,<%= @run.attempts or 0 %>
<% @run.splits.each do |split| %>
<%= split.title %>,<%= Time.at(split.finish_time).utc.strftime("%H:%M:%S.%2N").strip %>,<%= Time.at(split.duration).utc.strftime("%H:%M:%S.%2N").strip %>
<% end %>

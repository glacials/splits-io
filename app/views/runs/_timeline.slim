- scale_to ||= run.time
- cache [:timeline, :v3, {run: run, scale_to: scale_to}] do
  .timeline
    - run.collapsed_splits.each_with_index do |split, index|
      .pure-u.split id="#{run.id}-split-#{index}" class=next_timeline_color(run.id) style='width: 0;'
        .title = split.name
        .time.needs-formatting = split.duration
    - if run.time < scale_to
      .pure-u.split.disabled style="width: #{(scale_to - run.time) / scale_to * 100}%; z-index: #{run.collapsed_splits.count};"

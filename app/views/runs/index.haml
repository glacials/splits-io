- title "hello"
- content_for(:header) do
  %h2 splits i/o
  %h5
    upload
    - if user_signed_in? && current_user.name.present?
      %a{href: user_path(current_user)} all
    - else
      %u all
    the splits
  = render partial: 'shared/alerts'
- if user_signed_in? && current_user.runs.present?
  %article
    - cols = {with: [:owner_controls], without: [:runner]}
    - cache [:run_table, :v3, {user: current_user, cols: cols, page: params[:page] || 1}, order: [params[:by], params[:order]]] do
      = render partial: 'shared/run_table', locals: {runs: current_user.pbs, cols: cols}
- if user_signed_in?
  %article
    %div{id: ab_test(:show_follows) ? "follows" : ""}
- unless user_signed_in?
  %article
    Save your splits to a file using your favorite splitting program, then drag that file anywhere on this page.

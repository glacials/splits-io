= current_user.twitch_follows.that_run(category).find_each do |potential_rival|
  .col-md-3
    .card = link_to(rivalries_path(category: category, target: potential_rival), method: :post)
      = image_tag(potential_rival.avatar, skip_pipeline: true, class: 'card-img-top')
      .card-body
        h5 = potential_rival
      .card-footer
        - run = potential_rival.pb_for(category)
        - timing = run.default_timing
        span = Duration.new(run.duration_ms(timing)).format
        small< Personal Best
        br
        span = Duration.new(run.sum_of_best_ms(timing)).format
        small< Sum of Best

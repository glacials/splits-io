.row
  .col-lg-5.col-md-5
    .card
      = image_tag(rivalry.from_user.avatar, class: 'card-img-top')
      .card-body
        h5 = rivalry.from_user.twitch_display_name
      .card-footer
        - run = rivalry.from_user.pb_for(rivalry.category)
        - timing = params[:timing] || run.default_timing
        - if run.present?
          span = format_ms(run.duration_ms(timing))
        - else
          span = '-'
        small< Personal Best
        br
        - if run.present?
          span = format_ms(run.sum_of_best_ms(timing))
          small< Sum of Best
        - else
          span = '-'
  .col-lg-2.col-md-2
    h4 vs.
  .col-lg-5.col-md-5
    .card
      = image_tag(rivalry.to_user.avatar, class: 'card-img-top')
      .card-body
        h5 = rivalry.to_user.twitch_display_name
      .card-footer
        - run = rivalry.to_user.pb_for(rivalry.category)
        - timing = params[:timing] || run.default_timing
        - if run.present?
          span = format_ms(run.duration_ms(timing))
        - else
          span = '-'
        small< Personal Best
        br
        - if run.present?
          span = format_ms(run.sum_of_best_ms(timing))
          small< Sum of Best
        - else
          span = '-'
